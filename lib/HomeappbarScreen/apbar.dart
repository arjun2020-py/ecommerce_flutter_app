import 'package:flutter/material.dart';

class AppbrScreen extends StatelessWidget {
  const AppbrScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
